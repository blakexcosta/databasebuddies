html
head
    title Person
    body
        //just remember that this actually directs to /person, not /, because this page is already hosted on /person
        form(action="/", method="POST")
        div
            label(for="name") Name:
            input(name="name")
        br
        div
            label(for="age") Age:
            input(name="age")
        br
        div
            label(for="nationality") Nationality:
            input(name="nationality")
        br
        button(type="submit") Create a new person
